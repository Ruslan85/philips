<%= form_for task, remote: true do |f| %>

  <%= f.check_box :complete %><br>
  <%= f.submit "Update" %>
  <%= f.label :complete, task.name %><br>
  <%= link_to "(remove)", task, method: :delete, data: {confirm: "What are you doing fucking idiot???"}, remote: true %>
<% end %>
